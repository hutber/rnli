include ../mixins
if status && typeof status !== typeof undefined
    +postcodebox(result[0], {})
    //- each val in result
else if(length)
    .box.large.white Please enter a valid postcode
else
    .box.large.white Sorry No results