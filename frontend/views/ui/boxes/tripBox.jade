mixin tripBox(data)
   - var currentCatchItem = function(){
   -     var i = 0;
   -     while (i < RN.user.get('catch').length) {
   -        if (i == 3) {
   -           break;
   -        }
   -        i += 1;
   -     }
   - };
   .box.tripHomeBox
         .image
            if(typeof data.image !=="undefined")
               img(src='img/')
            else
               img(src='img/placeholder.gif')
         .text
            h3 #{data.species}
            .item W
               span #{data.weight1}lb #{data.weight2}oz
            .item L
               span #{data.height1}ft #{data.height2}in
         .icon
            i(class='icon-right-open')