mixin tripBox(data, date)
   - var currentCatchItem = function(){
   -     var i = 0;
   -     while (i < RN.user.get('catch').length) {
   -        if (i == 3) {
   -           break;
   -        }
   -        i += 1;
   -     }
   - };
   .box.tripHomeBox(data-id= data.id)
         .image
            if(typeof data.image === 1)
               img(src='http://' + RN.glb.url.cdn + '/uploads/catch/' + RN.user.get('uid') + '/' +  data.id + '/' + 'catch.jpg')
            else
               img(src='img/placeholder.gif')
         .text
            h3 #{data.name}
            .item #{date}
            +rating(data.rating, 'disabled')
         .icon
            i(class='icon-right-open')