mixin map(data, plain)
    .map
        if(!plain)
            .info
                .wind
                    i(class='icon-right-circled')
                    | #{data.winddirection} #{data.windgust}mph
                .current
                    i(class='icon-right-circled')
                    | No Data
                .enlargetrip
                    i(class='icon-zoom-in-outline')
                    | ENLARGE
        div(id="map-canvas", style="width:100%; height:170px;")