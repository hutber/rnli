mixin input(data)
    - data.type = data.type || 'text'
    .control-group
        .icon-right-open
        input(placeholder="#{data.place}", type="#{data.type}", name="#{data.name}", value="#{data.value}", class=data.class)